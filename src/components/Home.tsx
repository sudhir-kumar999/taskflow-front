import Box from "@mui/material/Box";

const Home = () => {
  return (
    <Box
      sx={{
        marginTop: "70px",
        ml: { md: "-250px" },
        p: 2,
        display: "flex",
        flexDirection: "column",
      }}
    >
      home page
    </Box>
  );
};

export default Home;
